import React from 'react';
const Meal = (props) => {
    let {name,tags,category,area,instrctions,ingredients,image} = props;
    
    return ( <div className="meal-wrapper">

    </div> );
}
 
export default Meal;